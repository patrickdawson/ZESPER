/* tslint:disable:no-unused-variable */

import {Food} from './food';

describe('Food', () => {
  it('should create an instance', () => {
    expect(new Food('Leberkäs', 1.80)).toBeTruthy();
  });
});
